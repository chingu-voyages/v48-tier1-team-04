import "./searchBar.styles.scss"
import filterDinosaursByName from "../../utils/filterDinosaurs"

//const searchBar = document.getElementById('search-bar')
const dinoList = document.getElementById('dino-list')


//Pagination should be definetly here
const createSearchBar = () => {
    //Still not creating it and appending it, but giving it its functionality
    //searchBar.addEventListener('input', updateDinoList)
}


export default createSearchBar